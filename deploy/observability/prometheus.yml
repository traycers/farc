# Scrape config for the farc/hls_server /metrics endpoints
# (.scratch/observability/spec.md).
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: farc
    static_configs:
      - targets: ["farc:9090"]

  - job_name: hls_server
    static_configs:
      - targets: ["hls_server:9091"]
