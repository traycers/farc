Смотрел маркеры «открытый вопрос»/«техдолг»/«предмет отдельной спецификации» по всем документам и статусы ADR — вот что реально нужно закрыть (или хотя бы явно решить/отложить) до плана:

**A. ~~Затрагивают ядро v1, но статус ADR — «Предложено», не «Принято»~~ — закрыто:**
- ~~**ADR-011** (квота на чтение при непрерывной записи)~~ — принят как есть (детерминированная квота `M`/`K`, без изменений в «Решении»).
- ~~**ADR-012** (защита retention от скачков системных часов)~~ — принят в упрощённом для v1 варианте: простой `now()` без защиты от аномалий; полная защита (`effective_now()`, персистентный `last_effective_time`) — целевое решение, отложено до отдельной доработки.

**B. ~~Тривиальный техдолг~~ — закрыто:** ~~правка псевдокода `select_next_index` под `write_mode`~~ — внесена в `04-storage-operations.md` §6.2/§6.3/§6.4; попутно исправлено расхождение имени параметра между документами (`03-storage-format.md` называл его `recording.mode`/`"fill_once"`, `11-service-composition.md` — `write_mode`/`"fill_until_full"`) — канонизировано как `write_mode`/`"fill_until_full"` везде (03, 04, 11).

**C. Реальные дырки в спецификации, блокирующие конкретные компоненты v1:**
- Нет ни одного документа с HTTP-маршрутами/кодами ответов и форматом WebSocket-сообщений — четыре места в докстве («10-capture-policy.md», «11-service-composition.md») откладывают это на «отдельную спецификацию API», которой не существует. Без неё не реализовать `HttpApiServer`/`EventPushServer` даже в минимальном объёме.
- Канал сигнала backpressure `StorageUnit → CapturePolicy` не определён (`10-capture-policy.md` §8) — а это часть основного write-пути (цикл буфера Recorder↔ChannelIngest), не второстепенная фича, значит для v1 её нельзя просто отложить как GeometryManager.
- Схема навёрстывания пропущенных compact-событий при разрыве WebSocket (открытый вопрос ADR-015) — здесь, наоборот, можно осознанно отложить на v1 («не гарантируем доставку, догоняем только через TOC-fallback»), но это тоже стоит зафиксировать явно.

**D. ~~Известные баги в `internal/traa`~~ — неактуально:** пакет `traa` целиком удалён (коммит «remove traa package», 2026-07-23), `internal/` сейчас пуст. Баги (`balance.go` out-of-bounds, `calc_depth.go` debug-принт/неограниченный цикл) исчезли вместе с кодом, а не были исправлены — SoA-структура каталога (ADR-009) не реализована вообще, писать её для v1 придётся с нуля.

**E. ~~MVP-scope решения по аналогии с уже принятым по `JobRunner`~~ — закрыто, все три «да»:**
- RTSP-приём (`IngestManager`/`ChannelIngest`/`CapturePolicy`) — в v1.
- `Catalog` на SSD — в v1.
- `EventPushServer`/WebSocket-push — в v1.

**F. ~~Формат файла конфигурации~~ — закрыто:** конкретная JSON-схема зафиксирована в `04-storage-operations.md` §2.1 (пример + пояснения). Ключевые решения: `write_mode`/`retention.days` не в этом файле (живут в самом Storage, задаются при инициализации); канал→хранилище — полем `storage` в записи канала, а не списком каналов в записи хранилища; временные величины (`prerecord`/`postrecord`/…) — строка-длительность Go-стиля (`"30s"`, `"1h"`), не число секунд.

Все пункты (A–F) закрыты.
